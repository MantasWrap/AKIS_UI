import React from 'react';

export function TrainingStudioModelsPage() {
  return (
    <section className="ts-page">
      <header className="ts-page-header">
        <div>
          <h2>Models &amp; Jobs</h2>
          <p>Model registry and training jobs (placeholder for Phase D).</p>
        </div>
      </header>
      <p className="ts-helper">
        This page will show models trained on Training Studio datasets, training
        jobs, and basic metrics. For now it is a placeholder so routing and
        layout are wired correctly.
      </p>
    </section>
  );
}
